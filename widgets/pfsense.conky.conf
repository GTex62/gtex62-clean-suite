-- pfSense Conky widget (Lua-drawn). Loads lua/pf_widget.lua and draws via lua_draw_hook_pre.

conky.config = {
  alignment              = 'top_left',
  background             = false,
  double_buffer          = true,
  update_interval        = 1,

  use_xft                = true,
  font                   = 'DejaVu Sans Mono:size=12',

  own_window             = true,
  own_window_type        = 'desktop',
  own_window_hints       = 'undecorated,sticky,skip_taskbar,skip_pager,below',
  own_window_argb_visual = true,
  own_window_argb_value  = 0,
  own_window_transparent = true,
  own_window_class       = 'Conky',

  gap_x                  = 3807,
  gap_y                  = 740,

  default_color          = 'FFFFFF',
  color1                 = 'A0A0A0',

  minimum_width          = 640,
  minimum_height         = 720,

  lua_load               = os.getenv("HOME") .. '/.config/conky/gtex62-clean-suite/lua/pf_widget.lua',
  lua_draw_hook_pre      = 'pf_draw',
}

conky.text = [[
# Lua-drawn widget (intentionally no Conky text output)
]]
